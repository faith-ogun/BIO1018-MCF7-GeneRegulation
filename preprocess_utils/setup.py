# setup.py

from setuptools import setup, find_packages

setup(
    name='preprocess_utils',
    version='0.1',
    packages=find_packages(),  # This will automatically find your preprocess_utils package
)